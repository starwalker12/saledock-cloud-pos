import { SaleDockLoading } from "@/components/loading/saledock-loading";

export default function LoginLoading() {
  return (
    <SaleDockLoading
      title="Loading SaleDock..."
      description="Opening the secure sign-in screen."
    />
  );
}
