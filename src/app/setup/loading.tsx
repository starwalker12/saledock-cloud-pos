import { SaleDockLoading } from "@/components/loading/saledock-loading";

export default function SetupLoading() {
  return (
    <SaleDockLoading
      title="Setting up your session..."
      description="Preparing SaleDock for your shop."
    />
  );
}
